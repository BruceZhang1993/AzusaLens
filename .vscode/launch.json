{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Debug Azusa Lens",
      "cargo": {
        "args": ["build", "--package", "azusa-lens-desktop"],
        "filter": {
          "name": "azusa-lens-desktop",
          "kind": "bin"
        }
      },
      "args": [],
      "cwd": "${workspaceFolder}"
    }
  ]
}
