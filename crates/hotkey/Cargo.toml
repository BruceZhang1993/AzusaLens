[package]
name = "azusa-hotkey"
version.workspace = true
edition.workspace = true
rust-version.workspace = true

[dependencies]
wayclip-global-hotkey = { version = "=0.7.0", default-features = false, features = ["async-io"] }
